/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  // basePath: '/chatgpt-prompt-builder',
  // assetPrefix: '/chatgpt-prompt-builder/',
};

module.exports = nextConfig;